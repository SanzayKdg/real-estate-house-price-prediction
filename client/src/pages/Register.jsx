import RegisterForm from "../components/RegisterForm";

const Register = () => {
  return (
    <main
      style={{
        marginTop: "4rem",
        border: "1px solid transparent",
      }}
    >
      <RegisterForm />
    </main>
  );
};

export default Register;
