import LoginForm from "../components/LoginForm";

const Login = () => {
  return (
    <main
      style={{
        marginTop: "4rem",
        border: "1px solid transparent",
      }}
    >
      <LoginForm />
    </main>
  );
};

export default Login;
